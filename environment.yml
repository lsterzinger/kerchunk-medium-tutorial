name: fsspec-tutorial
channels:
        - conda-forge
dependencies:
        - python==3.8
        - git
        - pip
        - fsspec
        - matplotlib
        - numpy
        - s3fs
        - tqdm
        - xarray
        - jupyterlab
        - h5py<3.2
        - zarr
        - dask
        - pip:
            - numcodecs>=0.8.0
            - git+https://github.com/martindurant/fsspec-reference-maker@grib
        #     - "--editable=git+https://github.com/intake/fsspec-reference-maker"
